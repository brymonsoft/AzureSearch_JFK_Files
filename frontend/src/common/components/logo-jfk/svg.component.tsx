import * as React from 'react';
const styles = require('./style.scss');
const finIcon = require('../../../assets/img/fin-icon.png');

interface Props {
  className: string;
}

export const JFKSvg: React.StatelessComponent<Props> = (props) => (
    <h1>
        Cognitive Finance Search
    </h1>
)
